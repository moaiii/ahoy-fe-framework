import * as types from './types';

const initalState = {};

const landingPageReducer = (state = initalState, action) => {
  switch (action.type) {
    case /*types.CHANGE_CONTAINER_HEIGHT*/x:
      return 
  };

  return state;
}

export default landingPageReducer;
